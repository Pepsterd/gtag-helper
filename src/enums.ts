export enum EVENT_HANDLER_ATTRIBUTE_NAME {
    click = 'click',
}

export enum EVENT_HANDLER_ATTRIBUTE_EVENT_TYPE {
    click = 'click',
}

export enum GETTER_HELPER {
    innerText = 'Innertext',
    innerHtml = 'Innerhtml',
    classes = 'Classes',
    customPayload = 'Custompayload',
}

export enum GTAG_HELPER {
    prefix = 'gtag-helper',
}
